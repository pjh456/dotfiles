#!/bin/dash
last=""
while sleep 10; do
    raw=$(tlp-stat -s 2>/dev/null | grep -oP 'TLP profile\s*=\s*\K\S+' || echo "balanced/BAT")
    [ "$raw" != "$last" ] && "$(dirname "$0")/mpv-profile-switch"
    last="$raw"
done
